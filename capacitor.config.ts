import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vitalsync.app',
  appName: 'VitalSync',
  webDir: 'dist/vitalsyncdef/browser'
};

export default config;
